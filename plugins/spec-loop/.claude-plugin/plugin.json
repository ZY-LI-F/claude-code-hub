{
  "name": "spec-loop",
  "version": "0.3.0",
  "description": "Spec-driven nested loop. v0.3: tests-first convergence, multi-signal codex-done detection, auto-advance waves + auto-rescue failed tasks, cherry-pick merge fallback, strict global-review prompt with drift retry, new /spec-rebind + /spec-resume commands. v0.2 multi-mode entry /spec-loop:multi-start preserved.",
  "author": { "name": "spec-loop contributors" }
}
